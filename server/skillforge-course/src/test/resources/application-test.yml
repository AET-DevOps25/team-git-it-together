spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: test_course_db
  profiles:
    active: test

logging:
  level:
    com.gitittogether.skillForge.server.course: DEBUG
    org.springframework.data.mongodb: DEBUG 